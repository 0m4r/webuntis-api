{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "examples",
  "projectType": "application",
  "root": "apps/examples",
  "sourceRoot": "apps/examples/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "dependsOn": ["^build"],
      "options": {
        "command": "rollup -c apps/examples/rollup.config.mjs"
      }
    }
  },
  "tags": [],
  "implicitDependencies": ["webuntis-api"]
}
